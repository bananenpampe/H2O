#


#1st test, test that a model can be properly initialized

#2nd test, test that function is sum_over_samples if aggregation is "sum"
#3rd test, test that function is mean_over_samples if aggregation is "mean"
#4th test, test that function is lambda if aggregation is "passthrough" 